services:
  db:
     image: postgres:18.3
     restart: always
     ports:
       - 5432:5432
     environment:
       POSTGRES_USER: admin
       POSTGRES_PASSWORD: postgres
       POSTGRES_DB: notes
     volumes:
       - pgdata:/var/lib/postgresql/18/docker

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080

volumes:
  pgdata:
